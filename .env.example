# Copy to `.env` in the project root (gitignored). Used by docker compose and local tooling.
# Generate a production secret with: mix phx.gen.secret
SECRET_KEY_BASE=

# Optional overrides for docker-compose.yml
# APP_PORT=4000
# OLLAMA_MODEL=llama3
